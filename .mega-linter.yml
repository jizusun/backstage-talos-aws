# Configuration file for MegaLinter
#
# See all available variables at https://megalinter.io/latest/config-file/ and in
# linters documentation
---
# all, none, or list of linter keys
APPLY_FIXES: all

# If you use ENABLE variable, all other languages/formats/tooling-formats will
# be disabled by default
# ENABLE:

# If you use ENABLE_LINTERS variable, all other linters will be disabled by
# default
# ENABLE_LINTERS:

DISABLE:
  - COPYPASTE # Comment to enable checks of excessive copy-pastes
  - SPELL # Comment to enable checks of spelling mistakes

DISABLE_LINTERS:
  - TERRAFORM_TERRASCAN # Doesn't support optional(type, default) OpenTofu syntax

MARKDOWN_DEFAULT_STYLE: rumdl
SHOW_ELAPSED_TIME: true

# tflint: use project config for correct plugins
TERRAFORM_TFLINT_CONFIG_FILE: terraform/.tflint.hcl

# zizmor: run offline to avoid GitHub API rate limits in CI
ACTION_ZIZMOR_ARGUMENTS: "--offline"

# Uncomment if you want MegaLinter to detect errors but not block CI to pass
# DISABLE_ERRORS: true
