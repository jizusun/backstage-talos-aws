#!/usr/bin/env bun
//MISE description="E2E tests: real AWS apply + verification (Terratest)"
//MISE depends=["lint"]
import { $ } from "bun";

console.log("═══ E2E Tests (real AWS) ═══");
console.log("Running Terratest E2E: apply → verify → destroy");
await $`cd terraform/tests && E2E=1 go test -v -run TestE2E -timeout 30m`;
console.log("\n✅ E2E tests passed");
