replicaCount: 1

image:
  repository: ghcr.io/backstage/backstage
  tag: latest

postgresql:
  enabled: true
  auth:
    postgresPassword: backstage
    database: backstage

resources:
  requests:
    cpu: 100m
    memory: 256Mi
  limits:
    cpu: 500m
    memory: 512Mi
